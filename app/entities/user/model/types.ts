export interface UserProfile {
  firstName: string;
  lastName: string;
  email: string;
  avatarLetter: string;
}

export interface ProfileStat {
  value: string;
  label: string;
}

export interface JobInfoItem {
  label: string;
  value: string;
}
